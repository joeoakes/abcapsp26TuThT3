# Build command for redis_https (run from this folder)
gcc -O2 -Wall -Wextra -std=c11 maze_https_mongo.c -o maze_https_redis \
  $(pkg-config --cflags --libs libmicrohttpd gnutls libbson-1.0 hiredis)

